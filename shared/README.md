# Shared Directory

This directory is reserved for contracts shared between the frontend and backend.

- `constants/`: Shared constants.
- `types/`: Shared types (TypeScript models/interfaces).
- `schemas/`: Shared data schemas (JSON or Pydantic representations).
